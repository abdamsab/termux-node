import React from 'react';
import { Link } from 'react-router-dom';

const Homepage = () => {
    return (
        <div>
            <h2>Hello from Termux in Android</h2>
        </div>
    );
};

export default Homepage;
